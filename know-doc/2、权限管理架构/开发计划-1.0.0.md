# 权限管理系统开发计划

## 概述

根据 `2、权限管理架构README-0.0.2.md` 文档要求，当前 know-boot/know-vue/know-uniapp 架构基础功能已完成，但完整的动态权限控制体系需后续开发。

**当前完成度：约 60%**
**需开发：约 40%**

---

## 第一阶段：核心权限模块（优先级 🔴 高）

预计工时：2-3 周

### 1.1 权限表结构设计

```sql
-- 1.1.1 权限表
CREATE TABLE `sys_permission` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `parent_id` bigint DEFAULT 0 COMMENT '父权限ID',
  `name` varchar(100) NOT NULL COMMENT '权限名称',
  `code` varchar(100) NOT NULL COMMENT '权限编码(user:add)',
  `permission_type` varchar(20) DEFAULT 'button' COMMENT '权限类型(button/api/data)',
  `resource_type` varchar(20) DEFAULT 'button' COMMENT '资源类型(button/menu/api)',
  `path` varchar(255) DEFAULT NULL COMMENT '路由/接口路径',
  `component` varchar(255) DEFAULT NULL COMMENT '前端组件路径',
  `icon` varchar(50) DEFAULT NULL COMMENT '图标',
  `sort` int DEFAULT 0 COMMENT '排序',
  `status` tinyint DEFAULT 1 COMMENT '状态(0禁用/1启用)',
  `create_time` bigint DEFAULT NULL COMMENT '创建时间',
  `update_time` bigint DEFAULT NULL COMMENT '更新时间',
  `del_flag` tinyint DEFAULT 0 COMMENT '删除标志',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_code` (`code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='权限表';

-- 1.1.2 角色-权限关联表
CREATE TABLE `sys_role_permission` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `role_id` bigint NOT NULL COMMENT '角色ID',
  `permission_id` bigint NOT NULL COMMENT '权限ID',
  `create_time` bigint DEFAULT NULL COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_role_id` (`role_id`),
  KEY `idx_permission_id` (`permission_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色-权限关联表';

-- 1.1.3 菜单-权限关联表
CREATE TABLE `sys_menu_permission` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `menu_id` bigint NOT NULL COMMENT '菜单ID',
  `permission_id` bigint NOT NULL COMMENT '权限ID',
  PRIMARY KEY (`id`),
  KEY `idx_menu_id` (`menu_id`),
  KEY `idx_permission_id` (`permission_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜单-权限关联表';

-- 1.1.4 数据权限配置表（可选）
CREATE TABLE `sys_data_permission` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `role_id` bigint NOT NULL COMMENT '角色ID',
  `dept_ids` varchar(500) DEFAULT NULL COMMENT '可见部门ID列表',
  `data_scope` tinyint DEFAULT 1 COMMENT '数据范围(1全部/2本部门/3本部门及子部门/4仅本人)',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='数据权限配置表';
```

### 1.2 后端实体类

```
位置: know-boot-common/src/main/java/com/know/knowboot/entity/tenant/
├── SysPermission.java        # 权限实体（新增）
├── SysRolePermission.java   # 角色-权限关联（新增）
├── SysMenuPermission.java   # 菜单-权限关联（新增）
├── SysDataPermission.java  # 数据权限配置（新增）
```

### 1.3 后端 Mapper

```
位置: know-boot-common/src/main/java/com/know/knowboot/mapper/tenant/
├── SysPermissionMapper.java
├── SysRolePermissionMapper.java
├── SysMenuPermissionMapper.java
└── SysDataPermissionMapper.java
```

### 1.4 后端 Service

```
位置: know-boot-system/src/main/java/com/know/knowboot/service/
├── ISysPermissionService.java + impl
├── ISysRolePermissionService.java + impl
└── ISysDataPermissionService.java + impl
```

### 1.5 后端 Controller

```
位置: know-boot-system/src/main/java/com/know/knowboot/controller/
├── SysPermissionController.java  # 权限管理 CRUD
└── SysRolePermissionController.java # 角色分配权限
```

### 1.6 动态权限校验拦截器

```java
位置: know-boot-system/src/main/java/com/know/knowboot/filter/
JwtPermissionFilter.java

职责:
1. 解析 JWT Token 获取用户信息和权限列表
2. 从数据库加载用户权限（带缓存）
3. 校验接口访问权限
4. 放行或返回 403
```

---

## 第二阶段：前端权限控制（优先级 🔴 高）

预计工时：1-2 周

### 2.1 Vue 权限指令

```typescript
位置: know-vue/src/directives/permission.ts

实现:
// v-permission="'user:add'" - 显示/隐藏
// v-disable="'user:edit'" - 禁用但显示
export const permission = {
  mounted(el, binding) {
    const hasPermission = checkPermission(binding.value)
    if (!hasPermission) {
      el.parentNode?.removeChild(el)
    }
  }
}
```

### 2.2 权限工具函数

```typescript
位置: know-vue/src/utils/permission.ts

实现:
// 检查是否有权限
export function hasPermission(code: string): boolean

// 获取用户所有权限
export function getUserPermissions(): string[]

// 权限路由守卫
export function setupPermissionGuard(router)
```

### 2.3 用户信息存储改造

```typescript
位置: know-vue/src/stores/modules/user.ts

改造:
- 登录成功后获取用户权限列表
- 存储到 Pinia
- 路由守卫使用
```

### 2.4 动态路由生成

```typescript
位置: know-vue/src/router/

实现:
// 根据后端返回的菜单动态生成路由
// 根据权限过滤可访问的路由
```

---

## 第三阶段：日志审计模块（优先级 🟡 中）

预计工周：1 周

### 3.1 操作日志表

```sql
CREATE TABLE `sys_oper_log` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `title` varchar(100) DEFAULT '' COMMENT '操作模块',
  `business_type` tinyint DEFAULT 0 COMMENT '业务类型',
  `method` varchar(100) DEFAULT '' COMMENT '方法名',
  `request_method` varchar(10) DEFAULT '' COMMENT '请求方式',
  `operator_type` tinyint DEFAULT 0 COMMENT '操作类型',
  `oper_name` varchar(50) DEFAULT '' COMMENT '操作人',
  `oper_ip` varchar(50) DEFAULT '' COMMENT '操作IP',
  `oper_location` varchar(255) DEFAULT '' COMMENT '操作位置',
  `oper_url` varchar(255) DEFAULT '' COMMENT '请求URL',
  `oper_param` text COMMENT '请求参数',
  `json_result` text COMMENT '返回结果',
  `status` tinyint DEFAULT 0 COMMENT '状态',
  `error_msg` text COMMENT '错误信息',
  `oper_time` bigint DEFAULT NULL COMMENT '操作时间',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志表';
```

### 3.2 登录日志表

```sql
CREATE TABLE `sys_login_log` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `username` varchar(50) DEFAULT '' COMMENT '用户名',
  `ipaddr` varchar(50) DEFAULT '' COMMENT '登录IP',
  `login_location` varchar(255) DEFAULT '' COMMENT '登录位置',
  `browser` varchar(50) DEFAULT '' COMMENT '浏览器',
  `os` varchar(50) DEFAULT '' COMMENT '操作系统',
  `status` tinyint DEFAULT 0 COMMENT '状态(0失败/1成功)',
  `msg` varchar(255) DEFAULT '' COMMENT '提示消息',
  `login_time` bigint DEFAULT NULL COMMENT '登录时间',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='系统访问记录表';
```

### 3.3 AOP 切面记录

```java
位置: know-boot-system/src/main/java/com/know/knowboot/aspect/
OperLogAspect.java

实现:
- @OperLog 注解定义
- 拦截 Controller 方法记录操作日志
```

---

## 第四阶段：安全增强模块（优先级 🟡 中）

预计工周：1 周

### 4.1 Token 续期机制

```java
// 1. Refresh Token 存储 Redis
// 2. /auth/refresh 接口实现
// 3. 前端自���刷新逻辑
```

### 4.2 Token 吊销机制

```java
// 1. Token 黑名单 Redis 存储
// 2. 登录/修改密码时加入黑名单
// 3. 过滤器校验黑名单
```

### 4.3 密码安全策略

```java
// 1. 密码加密强度配置
// 2. 连续错误锁定逻辑
// 3. 密码过期提醒
```

---

## 第五阶段：租户权限增强（优先级 🟡 中）

预计工周：1 周

### 5.1 租户数据隔离

```java
// 1. MyBatis Plus 租户拦截器
// 2. INSERT/UPDATE 自动注入 tenant_id
// 3. WHERE 条件自动过滤其他租户数据
```

### 5.2 跨租户访问控制

```java
// 1. 超级管理员角色
// 2. 跨租户查询标识
// 3. 审计日志记录
```

---

## 第六阶段：移动端适配（优先级 🟢 低）

预计工周：1 周

### 6.1 know-uniapp 权限适配

```
已确认：
- 底部导航动态配置
- 首页菜单动态配置

待开发：
- 权限指令适配
- 接口权限校验
```

---

## 开发资源清单

## 开发资源清单

### 当前代码库状态（搜索结果）

#### 后端 - 已实现的权限相关表

| 表名 | 实体类位置 | 状态 |
|------|----------|------|
| sys_tenant | know-boot-common/.../entity/tenant/SysTenant.java | ✅ |
| sys_user | know-boot-common/.../entity/tenant/SysUser.java | ✅ |
| sys_role | know-boot-common/.../entity/tenant/SysRole.java | ✅ |
| sys_menu | know-boot-common/.../entity/tenant/SysMenu.java | ✅ |
| sys_dept | know-boot-common/.../entity/tenant/SysDept.java | ✅ |
| sys_job | know-boot-common/.../entity/tenant/SysJob.java | ✅ |
| sys_dict / sys_dict_type | know-boot-common/.../entity/tenant/SysDict.java | ✅ |
| sys_user_role | know-boot-common/.../entity/tenant/SysUserRole.java | ✅ |
| sys_role_menu | know-boot-common/.../entity/tenant/SysRoleMenu.java | ✅ |
| **sys_permission** | **❌ 不存在** | ❌ |
| sys_user_tenant | know-boot-common/.../entity/tenant/SysUserTenant.java | ✅ |
| sys_tabbar | know-boot-common/.../entity/tenant/SysTabbar.java | ✅ |

#### 后端 - 日志相关（不存在）

| 功能 | 要求 | 状态 |
|------|------|------|
| sys_oper_log | 操作日志表 | ❌ 不存在 |
| sys_login_log | 登录日志表 | ⚠️ 基础存在 |

#### 前端 Vue - 权限相关

| 功能 | 文件位置 | 状态 |
|------|--------|------|
| 权限指令 | ❌ 不存在 | ❌ |
| 权限工具函数 | ❌ 不存在 | ❌ |
| 用户权限存储 | stores/modules/user.ts (无permissions字段) | ⚠️ 不完整 |
| 动态路由生成 | ❌ 不存在 | ❌ |

### 核心API清单

---

## 里程碑

| 阶段 | 里程碑 | 完成标准 |
|------|--------|----------|
| 第一阶段完成 | 核心权限模块 | 权限CRUD + 动态拦截 |
| 第二阶段完成 | 前端权限控制 | 按钮指令 + 路由守卫 |
| 第三阶段完成 | 日志审计 | 操作日志 + 登录日志 |
| 第四阶段完成 | 安全增强 | Token续期 + 吊销 |
| 第五阶段完成 | 租户隔离 | 数据隔离 + 跨租户 |

---

## 版本记录

| 版本 | 日期 | 内容 |
|------|------|------|
| 1.0.0 | 2026-04-24 | 初始开发计划 |