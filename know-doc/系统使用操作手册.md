# 系统使用操作手册

当前版本: 1.0.0

---

## 目录

1. [数据字典功能](#一数据字典功能)
2. [操作日志功能](#二操作日志功能)
3. [权限指令使用](#三权限指令使用)
4. [多租户功能](#四多租户功能)
5. [初始化数据](#五初始化数据)
6. [常见问题](#六常见问题)
7. [相关文件路径](#七相关文件路径)

---

## 一、数据字典功能

### 1.1 概述

数据字典功能用于管理系统中的静态数据，如用户性别、岗位状态、菜单状态等。采用类型+数据的两层结构设计。

### 1.2 数据库设计

#### 字典类型表 (sys_dict_type)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| dict_name | varchar(100) | 字典名称 |
| dict_type | varchar(100) | 字典类型（唯一标识） |
| remark | varchar(500) | 备注 |
| status | tinyint | 状态 [0=停用, 1=正常] |
| create_time | bigint | 创建时间 |
| update_time | bigint | 更新时间 |
| delete_time | bigint | 删除时间 |

#### 字典数据表 (sys_dict_data)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| dict_type_id | bigint | 字典类型ID |
| dict_label | varchar(100) | 字典标签 |
| dict_value | varchar(100) | 字典值 |
| dict_code | varchar(100) | 字典编码（业务使用） |
| sort | int | 排序 |
| status | tinyint | 状态 [0=停用, 1=正常] |
| remark | varchar(500) | 备注 |
| create_time | bigint | 创建时间 |
| update_time | bigint | 更新时间 |
| delete_time | bigint | 删除时间 |

### 1.3 后端API

#### 字典类型接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /system/dict/type/list | 获取字典类型列表 |
| GET | /system/dict/type/{id} | 获取字典类型详情 |
| POST | /system/dict/type | 新增字典类型 |
| PUT | /system/dict/type | 修改字典类型 |
| DELETE | /system/dict/type/{id} | 删除字典类型 |

#### 字典数据接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /system/dict/list | 获取字典数据列表 |
| GET | /system/dict/{id} | 获取字典数据详情 |
| POST | /system/dict | 新增字典数据 |
| PUT | /system/dict | 修改字典数据 |
| DELETE | /system/dict/{id} | 删除字典数据 |

### 1.4 know-vue 前端使用

#### API调用

```typescript
import { 
  getDictTypeList, 
  getDictList, 
  addDictType, 
  updateDictType, 
  deleteDictType,
  addDict,
  updateDict,
  deleteDict 
} from '@/api/system/dict'

// 获取字典类型列表
const typeList = await getDictTypeList()

// 根据类型ID获取字典数据
const dictData = await getDictList(dictTypeId)
```

#### 页面使用

参考 `src/views/system/dict.vue`

### 1.5 know-uniapp 前端使用

#### 字典Store

```typescript
import { useDictStore } from '@/stores/dict'

const dictStore = useDictStore()

// 获取选项列表（用于picker等）
const sexOptions = dictStore.getDictOptions('sys_user_sex')
// 返回: [{ label: '男', value: '0', ... }, { label: '女', value: '1', ... }]

// 根据值获取标签
const label = dictStore.getDictLabel('sys_user_sex', '0')
// 返回: '男'
```

#### 工具函数

```typescript
import { getDictLabel, getDictOptions, formatDict } from '@/utils/dict'

// 获取用户性别选项
const sexOptions = getSexOptions()
// 返回: [{ label: '男', value: '0' }, { label: '女', value: '1' }]

// 获取性别标签
const sexLabel = getSexLabel('0')
// 返回: '男'

// 格式化显示（当找不到时返回默认值 '-'）
const display = formatDict('sys_user_sex', '0', '-')
// 返回: '男'

const display2 = formatDict('sys_user_sex', '99', '-')
// 返回: '-'
```

#### 初始化自动加载

字典数据在 App.vue 中自动加载：

```typescript
// 登录后自动加载所有字典数据
if (userStore.isLogin) {
  await initData()  // 包含用户信息和字典数据
}
```

手动刷新：

```typescript
import { useDictStore } from '@/stores/dict'

const dictStore = useDictStore()
await dictStore.refreshDictData()
```

---

## 二、操作日志功能

### 2.1 概述

操作日志功能用于记录用户在系统中的操作行为，包括操作模块、请求参数、返回结果等。

### 2.2 数据库设计

#### 操作日志表 (sys_oper_log)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| title | varchar(100) | 操作模块 |
| business_type | tinyint | 业务类型 [0:其他, 1:新增, 2:修改, 3:删除, 4:查询] |
| method | varchar(100) | 方法名 |
| request_method | varchar(10) | 请求方式 (GET/POST/PUT/DELETE) |
| operator_type | tinyint | 操作类型 [0:其他, 1:后台用户, 2:手机端用户] |
| oper_name | varchar(50) | 操作人 |
| oper_ip | varchar(50) | 操作IP |
| oper_location | varchar(255) | 操作位置 |
| oper_url | varchar(255) | 请求URL |
| oper_param | text | 请求参数 |
| json_result | text | 返回结果 |
| status | tinyint | 状态 [0:正常, 1:异常] |
| error_msg | text | 错误信息 |
| oper_time | bigint | 操作时间 |
| tenant_id | bigint | 租户ID |

#### 登录日志表 (sys_login_log)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| username | varchar(50) | 用户名 |
| ipaddr | varchar(50) | 登录IP |
| login_location | varchar(255) | 登录位置 |
| browser | varchar(50) | 浏览器类型 |
| os | varchar(50) | 操作系统 |
| status | tinyint | 状态 [0:失败, 1:成功] |
| msg | varchar(255) | 提示消息 |
| login_time | bigint | 登录时间 |
| tenant_id | bigint | 租户ID |

### 2.3 后端实现

#### @OperLog 注解

使用 `@OperLog` 注解自动记录操作日志：

```java
@OperLog(value = "用户管理", businessType = 1)
@PostMapping
public AjaxResult add(@RequestBody SysUser user) {
    return AjaxResult.success(userService.add(user));
}
```

注解属性：

| 属性 | 类型 | 说明 |
|------|------|------|
| value | String | 操作模块 |
| businessType | int | 业务类型，默认0 |
| operDesc | String | 操作描述，默认空 |

#### 后端API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /system/log/oper/page | 分页查询操作日志 |
| GET | /system/log/oper/list | 获取操作日志列表 |
| DELETE | /system/log/oper/{id} | 删除操作日志 |
| DELETE | /system/log/oper/clear | 清空操作日志 |
| GET | /system/log/login/page | 分页查询登录日志 |
| GET | /system/log/login/list | 获取登录日志列表 |
| DELETE | /system/log/login/{id} | 删除登录日志 |
| DELETE | /system/log/login/clear | 清空登录日志 |

#### AuthController 登录日志

登录时自动记录登录日志：

```java
// 登录成功
saveLoginLog(username, ipaddr, 1, "登录成功", userAgent);

// 登录失败
saveLoginLog(username, ipaddr, 0, "密码错误", userAgent);

// 登出
saveLoginLog(username, ipaddr, 1, "退出成功", userAgent);
```

### 2.4 know-vue 前端使用

参考 `src/views/system/dict.vue` 类似的列表页面，使用 `/system/log/oper/*` 和 `/system/log/login/*` 接口。

### 2.5 know-uniapp 前端

uniapp端目前不直接展示日志管理页面，但登录日志会在后端自动记录。

---

## 三、权限指令使用

### 3.1 know-vue 权限指令

#### v-permission 指令

```vue
<template>
  <!-- 单个权限 -->
  <button v-permission="'system:user:add'">新增</button>
  
  <!-- 多个权限（满足其一即可显示） -->
  <button v-permission="['system:user:add', 'system:user:edit'">操作</button>
</template>
```

#### 编程式检查

```typescript
import { hasPermission } from '@/utils/permission'

// 检查单个权限
const canEdit = hasPermission('system:user:edit')

// 检查多个权限
const canOperate = hasPermission(['system:user:add', 'system:user:edit'])

if (canEdit) {
  // 执行操作
}
```

### 3.2 后端权限拦截器

JwtPermissionFilter 自动从数据库加载用户权限并校验：

```java
// 根据用户ID获取权限编码列表
List<String> permissionCodes = sysPermissionService.listCodesByUserId(userId);

// 权限校验
if (!hasPermission(permissionCodes, requiredPermission)) {
    return 403;
}
```

---

## 四、多租户功能

### 4.1 概述

多租户功能实现数据隔离，支持以下场景：
- 系统级租户（tenant_id = 0）：所有租户共享的数据
- 租户级数据：每个租户独立的数据
- 租户切换：用户可切换所属租户

### 4.2 数据库设计

#### 租户表 (sys_tenant)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| name | varchar(100) | 租户名称 |
| code | varchar(50) | 租户编码（唯一） |
| logo | varchar(255) | Logo地址 |
| status | tinyint | 状态 [0:停用, 1:正常] |
| expire_time | datetime | 过期时间 |
| create_by | varchar(32) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(32) | 更新人 |
| update_time | datetime | 更新时间 |
| del_flag | tinyint | 删除标志 |

#### 用户租户关联表 (sys_tenant_user)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| user_id | bigint | 用户ID |
| tenant_id | bigint | 租户ID |
| is_admin | tinyint | 是否租户管理员 [0:否, 1:是] |
| status | tinyint | 状态 [1:正常] |
| create_by | varchar(32) | 创建人 |
| create_time | bigint | 创建时间 |

#### 数据权限配置表 (sys_data_permission)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| role_id | bigint | 角色ID |
| dept_ids | varchar(500) | 可见部门ID列表 |
| data_scope | tinyint | 数据范围 [1:全部, 2:本部门, 3:本部门及子部门, 4:仅本人] |
| create_by | varchar(32) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(32) | 更新人 |
| update_time | datetime | 更新时间 |

### 4.3 核心组件

#### TenantContextHolder 租户上下文

```java
// 获取当前租户ID
Long tenantId = TenantContextHolder.getTenantId();

// 获取租户编码
String tenantCode = TenantContextHolder.getTenantCode();

// 是否租户管理员
boolean isAdmin = TenantContextHolder.isTenantAdmin();

// 是否系统级租户
boolean isSystem = TenantContextHolder.isSystemTenant();

// 清除上下文
TenantContextHolder.clear();
```

#### CustomTenantLineHandler 多租户SQL处理器

自动在SQL中注入 `tenant_id` 条件，实现数据隔离。

**不进行租户过滤的表：**
- sys_tenant（租户表）
- sys_dict_type（字典类型）
- sys_dict_data（字典数据）
- sys_permission（权限表）
- sys_role_permission（角色权限关联）
- sys_oper_log（操作日志）
- sys_login_log（登录日志）
- sys_user（用户表）
- sys_role（角色表）

### 4.4 后端API

#### 租户管理接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /tenant/page | 分页查询租户 |
| GET | /tenant/list | 获取启用的租户列表 |
| GET | /tenant/user/list | 获取用户可访问的租户列表 |
| GET | /tenant/current | 获取用户当前租户 |
| POST | /tenant/switch | 切换当前租户 |
| GET | /tenant/{id} | 获取租户详情 |
| POST | /tenant | 新增租户 |
| PUT | /tenant | 修改租户 |
| DELETE | /tenant/{id} | 删除租户 |
| POST | /tenant/assign | 授权用户到租户 |
| POST | /tenant/remove | 从租户移除用户 |

### 4.5 使用示例

#### 登录时设置租户上下文

```java
// 登录成功后
Long userId = user.getId();
SysTenant tenant = sysTenantService.getUserCurrentTenant(userId);
if (tenant != null) {
    TenantContextHolder.setTenant(
        tenant.getId(), 
        tenant.getCode(), 
        false
    );
}
```

#### 切换租户

```typescript
// 前端调用切换接口
await request.post({ url: '/tenant/switch', params: { tenantId: 123 } })

// 后端自动更新上下文
sysTenantService.setUserCurrentTenant(userId, tenantId);
```

### 4.6 know-uniapp 租户支持

```typescript
// 获取用户可访问的租户列表
export function getUserTenantList() {
    return request.get({ url: '/tenant/user/list' }, { isAuth: true })
}

// 切换租户
export function switchTenant(tenantId: number) {
    return request.post({ url: '/tenant/switch', params: { tenantId } }, { isAuth: true })
}
```

---

## 五、初始化数据

### 5.1 SQL初始化脚本

运行以下SQL文件初始化数据：

```sql
-- 权限管理（包含5张表）
source db/20260324/permission_data_init.sql

-- 租户数据
source db/20260324/tenant_data_init.sql

-- 数据字典（如有）
source db/xxxx/dict_data_init.sql
```

### 5.2 初始权限数据

| ID | 权限名称 | 权限编码 |
|----|----------|----------|
| 1 | 用户管理 | system:user |
| 101 | 用户列表 | system:user:list |
| 102 | 新增用户 | system:user:add |
| 103 | 编辑用户 | system:user:edit |
| 104 | 删除用户 | system:user:delete |
| 2 | 角色管理 | system:role |
| 201 | 角色列表 | system:role:list |
| ... | ... | ... |

### 5.3 初始租户数据

| 编码 | 名称 | 说明 |
|------|------|------|
| DEFAULT | 默认租户 | 系统默认租户 |
| DEV | 开发租户 | 开发环境使用 |
| TEST | 测试租户 | 测试环境使用 |

### 5.4 租户数据自动初始化

系统启动时自动初始化：
- 默认租户（DEFAULT）
- 超级管理员角色（SUPER_ADMIN）
- 普通用户角色（USER）

---

## 六、常见问题

### 6.1 字典数据不显示

1. 检查后端 `sys_dict_type` 和 `sys_dict_data` 表是否有数据
2. 检查know-uniapp是否已登录（未登录不加载字典）
3. 调用 `dictStore.refreshDictData()` 手动刷新

### 6.2 权限校验失败

1. 检查用户是否绑定了角色
2. 检查角色是否分配了权限
3. 检查用户ID是否为1（超级管理员自动放行）

### 6.3 日志未记录

1. 检查方法是否有 `@OperLog` 注解
2. 检查 AOP 切面是否生效
3. 检查数据库连接是否正常

### 6.4 租户数据未隔离

1. 检查 TenantContextHolder 是否正确设置
2. 检查 MyBatis Plus 插件是否正确配置
3. 检查 CustomTenantLineHandler 的忽略表列表

---

## 七、相关文件路径

### 后端

| 文件 | 路径 |
|------|------|
| 租户上下文 | know-boot-common/.../context/TenantContextHolder.java |
| 多租户处理器 | know-boot-system/.../config/CustomTenantLineHandler.java |
| MyBatis配置 | know-boot-system/.../config/MybatisPlusConfig.java |
| 租户实体 | know-boot-common/.../entity/tenant/SysTenant.java |
| 用户租户关联 | know-boot-common/.../entity/tenant/SysTenantUser.java |
| 租户Mapper | know-boot-common/.../mapper/tenant/SysTenantMapper.java |
| 租户Service | know-boot-system/.../service/ISysTenantService.java |
| 租户Controller | know-boot-system/.../controller/TenantController.java |
| 数据权限实体 | know-boot-common/.../entity/tenant/SysDataPermission.java |
| 操作日志实体 | know-boot-common/.../entity/tenant/SysOperLog.java |
| 登录日志实体 | know-boot-common/.../entity/tenant/SysLoginLog.java |
| 字典实体 | know-boot-common/.../entity/tenant/SysDict.java |
| 权限实体 | know-boot-common/.../entity/tenant/SysPermission.java |
| @OperLog注解 | know-boot-common/.../aop/OperLog.java |
| 操作日志切面 | know-boot-system/.../aspect/OperLogAspect.java |
| 权限过滤器 | know-boot-system/.../config/JwtPermissionFilter.java |
| 租户初始化器 | know-boot-system/.../init/TenantDataInitializer.java |

### know-vue

| 文件 | 路径 |
|------|------|
| 字典API | src/api/system/dict.ts |
| 权限API | src/api/system/permission.ts |
| 字典页面 | src/views/system/dict.vue |
| 权限页面 | src/views/system/permission.vue |
| 权限指令 | src/directives/permission.ts |
| 权限插件 | src/plugins/modules/permission.ts |

### know-uniapp

| 文件 | 路径 |
|------|------|
| 字典API | src/api/system/dict.ts |
| 认证API | src/api/auth.ts |
| 字典Store | src/stores/dict.ts |
| 用户Store | src/stores/user.ts |
| 字典工具 | src/utils/dict.ts |
| 认证工具 | src/utils/auth.ts |
| 常量定义 | src/enums/constantEnums.ts |
| 入口文件 | src/App.vue |

---

版本: 1.0.0
更新日期: 2026-04-24