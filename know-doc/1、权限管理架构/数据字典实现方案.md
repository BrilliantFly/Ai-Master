# 数据字典功能实现方案

当前版本： 1.0.0

---

## 一、数据库设计

### 1.1 字典类型表 (sys_dict_type)

```sql
CREATE TABLE `sys_dict_type` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `dict_name` varchar(100) NOT NULL COMMENT '字典名称',
  `dict_type` varchar(100) NOT NULL COMMENT '字典类型（唯一标识）',
  `remark` varchar(500) DEFAULT NULL COMMENT '备注',
  `status` tinyint DEFAULT 1 COMMENT '状态 [0=停用, 1=正常]',
  `create_time` bigint DEFAULT NULL COMMENT '创建时间',
  `update_time` bigint DEFAULT NULL COMMENT '更新时间',
  `delete_time` bigint DEFAULT NULL COMMENT '删除时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_dict_type` (`dict_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='字典类型表';
```

### 1.2 字典数据表 (sys_dict_data)

```sql
CREATE TABLE `sys_dict_data` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `dict_type_id` bigint NOT NULL COMMENT '字典类型ID',
  `dict_label` varchar(100) NOT NULL COMMENT '字典标签',
  `dict_value` varchar(100) NOT NULL COMMENT '字典值',
  `dict_code` varchar(100) DEFAULT NULL COMMENT '字典编码（业务使用）',
  `sort` int DEFAULT 0 COMMENT '排序',
  `status` tinyint DEFAULT 1 COMMENT '状态 [0=停用, 1=正常]',
  `remark` varchar(500) DEFAULT NULL COMMENT '备注',
  `create_time` bigint DEFAULT NULL COMMENT '创建时间',
  `update_time` bigint DEFAULT NULL COMMENT '更新时间',
  `delete_time` bigint DEFAULT NULL COMMENT '删除时间',
  PRIMARY KEY (`id`),
  KEY `idx_dict_type_id` (`dict_type_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='字典数据表';
```

### 1.3 初始数据

```sql
-- 字典类型
INSERT INTO `sys_dict_type` (`dict_name`, `dict_type`, `remark`, `status`) VALUES
('用户性别', 'sys_user_sex', '用户性别列表', 1),
('岗位状态', 'sys_post_status', '岗位启用状态', 1),
('菜单状态', 'sys_menu_status', '菜单启用状态', 1),
('系统开关', 'sys_switch', '系统开关配置', 1);

-- 字典数据 - 用户性别
INSERT INTO `sys_dict_data` (`dict_type_id`, `dict_label`, `dict_value`, `dict_code`, `sort`, `status`) VALUES
(1, '男', '0', 'male', 1, 1),
(1, '女', '1', 'female', 2, 1),
(1, '未知', '2', 'unknown', 3, 1);

-- 字典数据 - 岗位状态
INSERT INTO `sys_dict_data` (`dict_type_id`, `dict_label`, `dict_value`, `dict_code`, `sort`, `status`) VALUES
(2, '启用', '0', 'enabled', 1, 1),
(2, '禁用', '1', 'disabled', 2, 1);
```

---

## 二、后端实现

### 2.1 实体类

#### DictType.java
```java
@Data
@ApiModel("字典类型实体")
public class DictType implements Serializable {

    private static final long serialVersionUID = 1L;

    @TableId(value = "id", type = IdType.AUTO)
    @ApiModelProperty("ID")
    private Long id;

    @ApiModelProperty("字典名称")
    private String dictName;

    @ApiModelProperty("字典类型")
    private String dictType;

    @ApiModelProperty("备注")
    private String remark;

    @ApiModelProperty("状态 [0=停用, 1=正常]")
    private Integer status;

    @TableField(fill = FieldFill.INSERT)
    @ApiModelProperty("创建时间")
    private Long createTime;

    @TableField(fill = FieldFill.INSERT_UPDATE)
    @ApiModelProperty("更新时间")
    private Long updateTime;

    @TableLogic
    @ApiModelProperty("删除时间")
    private Long deleteTime;
}
```

#### DictData.java
```java
@Data
@ApiModel("字典数据实体")
public class DictData implements Serializable {

    private static final long serialVersionUID = 1L;

    @TableId(value = "id", type = IdType.AUTO)
    @ApiModelProperty("ID")
    private Long id;

    @ApiModelProperty("字典类型ID")
    private Long dictTypeId;

    @ApiModelProperty("字典标签")
    private String dictLabel;

    @ApiModelProperty("字典值")
    private String dictValue;

    @ApiModelProperty("字典编码")
    private String dictCode;

    @ApiModelProperty("排序")
    private Integer sort;

    @ApiModelProperty("状态 [0=停用, 1=正常]")
    private Integer status;

    @ApiModelProperty("备注")
    private String remark;

    @TableField(fill = FieldFill.INSERT)
    @ApiModelProperty("创建时间")
    private Long createTime;

    @TableField(fill = FieldFill.INSERT_UPDATE)
    @ApiModelProperty("更新时间")
    private Long updateTime;

    @TableLogic
    @ApiModelProperty("删除时间")
    private Long deleteTime;
}
```

### 2.2 DTO/VO

#### DictTypeDTO.java（新增/修改）
```java
@Data
@ApiModel("字典类型DTO")
public class DictTypeDTO {

    @ApiModelProperty("ID（修改时必填）")
    private Long id;

    @NotBlank(message = "字典名称不能为空")
    @ApiModelProperty("字典名称")
    private String dictName;

    @NotBlank(message = "字典类型不能为空")
    @ApiModelProperty("字典类型")
    private String dictType;

    @ApiModelProperty("备注")
    private String remark;

    @ApiModelProperty("状态 [0=停用, 1=正常]")
    private Integer status;
}
```

#### DictDataDTO.java（新增/修改）
```java
@Data
@ApiModel("字典数据DTO")
public class DictDataDTO {

    @ApiModelProperty("ID（修改时必填）")
    private Long id;

    @NotNull(message = "字典类型ID不能为空")
    @ApiModelProperty("字典类型ID")
    private Long dictTypeId;

    @NotBlank(message = "字典标签不能为空")
    @ApiModelProperty("字典标签")
    private String dictLabel;

    @NotBlank(message = "字典值不能为空")
    @ApiModelProperty("字典值")
    private String dictValue;

    @ApiModelProperty("字典编码")
    private String dictCode;

    @ApiModelProperty("排序")
    private Integer sort;

    @ApiModelProperty("状态 [0=停用, 1=正常]")
    private Integer status;

    @ApiModelProperty("备注")
    private String remark;
}
```

### 2.3 Service 接口

#### IDictTypeService.java
```java
public interface IDictTypeService extends IService<DictType> {

    /**
     * 分页查询字典类型
     */
    IPage<DictType> page(DictType query, Integer pageNum, Integer pageSize);

    /**
     * 新增字典类型
     */
    boolean add(DictTypeDTO dto);

    /**
     * 修改字典类型
     */
    boolean update(DictTypeDTO dto);

    /**
     * 删除字典类型
     */
    boolean delete(Long id);

    /**
     * 获取所有正常状态的字典类型
     */
    List<DictType> listNormal();
}
```

#### IDictDataService.java
```java
public interface IDictDataService extends IService<DictData> {

    /**
     * 分页查询字典数据
     */
    IPage<DictData> page(DictData query, Integer pageNum, Integer pageSize);

    /**
     * 根据字典类型查询字典数据
     */
    List<DictData> listByTypeId(Long dictTypeId);

    /**
     * 根据字典类型编码查询字典数据
     */
    List<DictData> listByTypeCode(String dictTypeCode);

    /**
     * 新增字典数据
     */
    boolean add(DictDataDTO dto);

    /**
     * 修改字典数据
     */
    boolean update(DictDataDTO dto);

    /**
     * 删除字典数据
     */
    boolean delete(Long id);
}
```

### 2.4 Service 实现

#### DictTypeServiceImpl.java
```java
@Service
public class DictTypeServiceImpl extends ServiceImpl<DictTypeMapper, DictType> implements IDictTypeService {

    @Override
    public IPage<DictType> page(DictType query, Integer pageNum, Integer pageSize) {
        LambdaQueryWrapper<DictType> wrapper = new LambdaQueryWrapper<>();
        wrapper.like(query.getDictName() != null, DictType::getDictName, query.getDictName())
               .eq(query.getDictType() != null, DictType::getDictType, query.getDictType())
               .eq(query.getStatus() != null, DictType::getStatus, query.getStatus())
               .orderByDesc(DictType::getCreateTime);
        return page(new Page<>(pageNum, pageSize), wrapper);
    }

    @Override
    public boolean add(DictTypeDTO dto) {
        // 校验字典类型唯一性
        long count = count(new LambdaQueryWrapper<DictType>()
                .eq(DictType::getDictType, dto.getDictType()));
        if (count > 0) {
            throw new ServiceException("字典类型已存在");
        }
        
        DictType entity = new DictType();
        BeanUtils.copyProperties(dto, entity);
        return save(entity);
    }

    @Override
    public boolean update(DictTypeDTO dto) {
        DictType entity = getById(dto.getId());
        if (entity == null) {
            throw new ServiceException("字典类型不存在");
        }
        
        // 如果修改了类型，检查唯一性
        if (!entity.getDictType().equals(dto.getDictType())) {
            long count = count(new LambdaQueryWrapper<DictType>()
                    .eq(DictType::getDictType, dto.getDictType()));
            if (count > 0) {
                throw new ServiceException("字典类型已存在");
            }
        }
        
        BeanUtils.copyProperties(dto, entity);
        return updateById(entity);
    }

    @Override
    public boolean delete(Long id) {
        // 删除该类型下的所有数据
        dictDataService.remove(new LambdaQueryWrapper<DictData>()
                .eq(DictData::getDictTypeId, id));
        return removeById(id);
    }

    @Override
    public List<DictType> listNormal() {
        return list(new LambdaQueryWrapper<DictType>()
                .eq(DictType::getStatus, 1)
                .orderByAsc(DictType::getSort));
    }
}
```

#### DictDataServiceImpl.java
```java
@Service
public class DictDataServiceImpl extends ServiceImpl<DictDataMapper, DictData> implements IDictDataService {

    @Cacheable(value = "dict", key = "#dictTypeId")
    @Override
    public List<DictData> listByTypeId(Long dictTypeId) {
        return list(new LambdaQueryWrapper<DictData>()
                .eq(DictData::getDictTypeId, dictTypeId)
                .eq(DictData::getStatus, 1)
                .orderByAsc(DictData::getSort));
    }

    @Cacheable(value = "dict", key = "#dictTypeCode")
    @Override
    public List<DictData> listByTypeCode(String dictTypeCode) {
        // 先获取字典类型
        DictType dictType = dictTypeService.getOne(new LambdaQueryWrapper<DictType>()
                .eq(DictType::getDictType, dictTypeCode));
        if (dictType == null) {
            return Collections.emptyList();
        }
        return listByTypeId(dictType.getId());
    }

    @CacheEvict(value = "dict", allEntries = true)
    @Override
    public boolean add(DictDataDTO dto) {
        DictData entity = new DictData();
        BeanUtils.copyProperties(dto, entity);
        return save(entity);
    }

    @CacheEvict(value = "dict", allEntries = true)
    @Override
    public boolean update(DictDataDTO dto) {
        DictData entity = new DictData();
        BeanUtils.copyProperties(dto, entity);
        return updateById(entity);
    }

    @CacheEvict(value = "dict", allEntries = true)
    @Override
    public boolean delete(Long id) {
        return removeById(id);
    }
}
```

### 2.5 Controller

#### DictTypeController.java
```java
@Api(tags = "字典类型管理")
@RestController
@RequestMapping("/system/dict/type")
public class DictTypeController {

    @Autowired
    private IDictTypeService dictTypeService;

    @ApiOperation("分页查询字典类型")
    @GetMapping("/page")
    public ResultMessage<IPage<DictType>> page(
            DictType query,
            @RequestParam(defaultValue = "1") Integer pageNum,
            @RequestParam(defaultValue = "10") Integer pageSize) {
        return ResultMessage.success(dictTypeService.page(query, pageNum, pageSize));
    }

    @ApiOperation("获取所有正常字典类型")
    @GetMapping("/list")
    public ResultMessage<List<DictType>> list() {
        return ResultMessage.success(dictTypeService.listNormal());
    }

    @ApiOperation("新增字典类型")
    @PostMapping
    public ResultMessage<Boolean> add(@RequestBody @Valid DictTypeDTO dto) {
        return ResultMessage.success(dictTypeService.add(dto));
    }

    @ApiOperation("修改字典类型")
    @PutMapping
    public ResultMessage<Boolean> update(@RequestBody @Valid DictTypeDTO dto) {
        return ResultMessage.success(dictTypeService.update(dto));
    }

    @ApiOperation("删除字典类型")
    @DeleteMapping("/{id}")
    public ResultMessage<Boolean> delete(@PathVariable Long id) {
        return ResultMessage.success(dictTypeService.delete(id));
    }
}
```

#### DictDataController.java
```java
@Api(tags = "字典数据管理")
@RestController
@RequestMapping("/system/dict/data")
public class DictDataController {

    @Autowired
    private IDictDataService dictDataService;

    @ApiOperation("分页查询字典数据")
    @GetMapping("/page")
    public ResultMessage<IPage<DictData>> page(
            DictData query,
            @RequestParam(defaultValue = "1") Integer pageNum,
            @RequestParam(defaultValue = "10") Integer pageSize) {
        return ResultMessage.success(dictDataService.page(query, pageNum, pageSize));
    }

    @ApiOperation("根据字典类型ID查询字典数据")
    @GetMapping("/type/{dictTypeId}")
    public ResultMessage<List<DictData>> listByTypeId(@PathVariable Long dictTypeId) {
        return ResultMessage.success(dictDataService.listByTypeId(dictTypeId));
    }

    @ApiOperation("根据字典类型编码查询字典数据")
    @GetMapping("/typeCode/{dictTypeCode}")
    public ResultMessage<List<DictData>> listByTypeCode(@PathVariable String dictTypeCode) {
        return ResultMessage.success(dictDataService.listByTypeCode(dictTypeCode));
    }

    @ApiOperation("新增字典数据")
    @PostMapping
    public ResultMessage<Boolean> add(@RequestBody @Valid DictDataDTO dto) {
        return ResultMessage.success(dictDataService.add(dto));
    }

    @ApiOperation("修改字典数据")
    @PutMapping
    public ResultMessage<Boolean> update(@RequestBody @Valid DictDataDTO dto) {
        return ResultMessage.success(dictDataService.update(dto));
    }

    @ApiOperation("删除字典数据")
    @DeleteMapping("/{id}")
    public ResultMessage<Boolean> delete(@PathVariable Long id) {
        return ResultMessage.success(dictDataService.delete(id));
    }
}
```

---

## 三、缓存设计

### 3.1 缓存策略

| 缓存 Key | 缓存内容 | 过期时间 | 失效时机 |
|----------|----------|----------|----------|
| dict:{dictTypeId} | 字典数据列表 | 30分钟 | 字典数据增删改 |
| dict:{dictTypeCode} | 字典数据列表 | 30分钟 | 字典数据增删改 |
| dict:all_types | 所有正常状态的字典类型 | 1小时 | 字典类型增删改 |

### 3.2 缓存配置

```java
@Configuration
@EnableCaching
public class CacheConfig {

    @Bean
    public CacheManager cacheManager(RedisConnectionFactory factory) {
        RedisCacheConfiguration config = RedisCacheConfiguration.defaultCacheConfig()
                .entryTtl(Duration.ofMinutes(30))
                .serializeKeysWith(RedisSerializationContext.SerializationPair.fromSerializer(
                        new StringRedisSerializer()))
                .serializeValuesWith(RedisSerializationContext.SerializationPair.fromSerializer(
                        new FastJson2JsonRedisSerializer<>(Object.class)))
                .disableCachingNullValues();

        return RedisCacheManager.builder(factory)
                .cacheDefaults(config)
                .withCacheConfiguration("dict",
                        RedisCacheConfiguration.defaultCacheConfig().entryTtl(Duration.ofMinutes(30)))
                .build();
    }
}
```

---

## 四、前端实现

### 4.1 API 层

#### dict.ts
```typescript
import request from '@/utils/request'

// 字典类型
export function getDictTypePage(params: any) {
  return request({
    url: '/system/dict/type/page',
    method: 'get',
    params
  })
}

export function getDictTypeList() {
  return request({
    url: '/system/dict/type/list',
    method: 'get'
  })
}

export function addDictType(data: any) {
  return request({
    url: '/system/dict/type',
    method: 'post',
    data
  })
}

export function updateDictType(data: any) {
  return request({
    url: '/system/dict/type',
    method: 'put',
    data
  })
}

export function deleteDictType(id: number) {
  return request({
    url: `/system/dict/type/${id}`,
    method: 'delete'
  })
}

// 字典数据
export function getDictDataPage(params: any) {
  return request({
    url: '/system/dict/data/page',
    method: 'get',
    params
  })
}

export function getDictDataByTypeId(dictTypeId: number) {
  return request({
    url: `/system/dict/data/type/${dictTypeId}`,
    method: 'get'
  })
}

export function getDictDataByTypeCode(dictTypeCode: string) {
  return request({
    url: `/system/dict/data/typeCode/${dictTypeCode}`,
    method: 'get'
  })
}

export function addDictData(data: any) {
  return request({
    url: '/system/dict/data',
    method: 'post',
    data
  })
}

export function updateDictData(data: any) {
  return request({
    url: '/system/dict/data',
    method: 'put',
    data
  })
}

export function deleteDictData(id: number) {
  return request({
    url: `/system/dict/data/${id}`,
    method: 'delete'
  })
}
```

### 4.2 字典工具函数

#### dictUtil.ts
```typescript
import { getDictDataByTypeCode } from '@/api/dict'

// 字典缓存
const dictCache = new Map<string, any[]>()

/**
 * 获取字典数据
 * @param dictTypeCode 字典类型编码
 * @param forceRefresh 是否强制刷新缓存
 */
export async function getDictData(dictTypeCode: string, forceRefresh = false): Promise<any[]> {
  if (!forceRefresh && dictCache.has(dictTypeCode)) {
    return dictCache.get(dictTypeCode)!
  }
  
  try {
    const res = await getDictDataByTypeCode(dictTypeCode)
    if (res.code === 200) {
      dictCache.set(dictTypeCode, res.data)
      return res.data
    }
  } catch (error) {
    console.error('获取字典数据失败:', error)
  }
  
  return []
}

/**
 * 获取字典标签
 * @param dictTypeCode 字典类型编码
 * @param dictValue 字典值
 */
export function getDictLabel(dictTypeCode: string, dictValue: string): string {
  const dictData = dictCache.get(dictTypeCode) || []
  const item = dictData.find((d: any) => d.dictValue === dictValue)
  return item ? item.dictLabel : dictValue
}

/**
 * 清除字典缓存
 */
export function clearDictCache(dictTypeCode?: string) {
  if (dictTypeCode) {
    dictCache.delete(dictTypeCode)
  } else {
    dictCache.clear()
  }
}
```

### 4.3 使用示例

#### 在表格中使用
```vue
<template>
  <a-table :columns="columns" :data-source="dataSource">
    <template #bodyCell="{ column, record }">
      <template v-if="column.key === 'status'">
        <a-tag :color="record.status === 1 ? 'green' : 'red'">
          {{ getDictLabel('sys_switch', String(record.status)) }}
        </a-tag>
      </template>
    </template>
  </a-table>
</template>

<script setup lang="ts">
import { getDictData, getDictLabel } from '@/utils/dictUtil'

// 页面加载时获取字典数据
onMounted(async () => {
  await getDictData('sys_user_sex')
  await getDictData('sys_switch')
})
</script>
```

#### 在表单中使用
```vue
<template>
  <a-form :model="form" :label-col="{ span: 4 }">
    <a-form-item label="性别">
      <a-select v-model:value="form.sex">
        <a-select-option 
          v-for="item in sexOptions" 
          :key="item.dictValue" 
          :value="item.dictValue"
        >
          {{ item.dictLabel }}
        </a-select-option>
      </a-select>
    </a-form-item>
  </a-form>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { getDictData } from '@/utils/dictUtil'

const sexOptions = ref<any[]>([])

onMounted(async () => {
  sexOptions.value = await getDictData('sys_user_sex')
})
</script>
```

---

## 五、接口列表

### 5.1 字典类型

| 接口 | 方法 | 说明 |
|------|------|------|
| /system/dict/type/page | GET | 分页查询 |
| /system/dict/type/list | GET | 获取所有正常类型 |
| /system/dict/type | POST | 新增 |
| /system/dict/type | PUT | 修改 |
| /system/dict/type/{id} | DELETE | 删除 |

### 5.2 字典数据

| 接口 | 方法 | 说明 |
|------|------|------|
| /system/dict/data/page | GET | 分页查询 |
| /system/dict/data/type/{dictTypeId} | GET | 按类型ID查询 |
| /system/dict/data/typeCode/{dictTypeCode} | GET | 按类型编码查询 |
| /system/dict/data | POST | 新增 |
| /system/dict/data | PUT | 修改 |
| /system/dict/data/{id} | DELETE | 删除 |

---

## 六、版本历史

| 版本 | 更新内容 |
|------|----------|
| 1.0.0 | 初始版本，包含完整前后端实现 |
