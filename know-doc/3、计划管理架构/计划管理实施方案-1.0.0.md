# 计划管理及日程管理系统 - 实施方案

当前版本： 1.0.0

---

## 一、实施概述

### 1.1 系统目标

实现一个集计划管理、日程管理、时间四象限管理于一体的个人效能管理系统，支持计划与日程联动，参考指尖时光 App 的核心功能。

### 1.2 实施周期

| 阶段 | 周期 | 累计 |
|------|------|------|
| 第一阶段：数据库与后端基础 | 1周 | 1周 |
| 第二阶段：核心功能开发 | 2周 | 3周 |
| 第三阶段：前端开发 | 2周 | 5周 |
| 第四阶段：联动与高级功能 | 1周 | 6周 |
| 第五阶段：测试与优化 | 1周 | 7周 |

---

## 二、第一阶段：数据库与后端基础（第1周）

### 2.1 数据库设计

#### 任务清单

- [ ] 创建数据库并初始化 Flyway 脚本
- [ ] 创建 sys_quadrant（四象限表）
- [ ] 创建 sys_plan_type（计划类型表）
- [ ] 创建 sys_plan（计划表）
- [ ] 创建 sys_schedule（日程表）
- [ ] 初始化四象限基础数据
- [ ] 初始化计划类型基础数据

#### SQL 脚本

```sql
-- 1. 四象限表
CREATE TABLE `sys_quadrant` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `name` varchar(20) NOT NULL,
  `code` varchar(20) NOT NULL,
  `color` varchar(20) NOT NULL,
  `description` varchar(200) DEFAULT NULL,
  `sort` int DEFAULT 0,
  `status` tinyint DEFAULT 1,
  `create_time` bigint,
  `update_time` bigint,
  `delete_time` bigint,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_code` (`code`)
);

-- 2. 计划类型表
CREATE TABLE `sys_plan_type` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `type_name` varchar(50) NOT NULL,
  `type_code` varchar(50) NOT NULL,
  `icon` varchar(100) DEFAULT NULL,
  `color` varchar(20) DEFAULT NULL,
  `sort` int DEFAULT 0,
  `status` tinyint DEFAULT 1,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_type_code` (`type_code`)
);

-- 3. 计划表
CREATE TABLE `sys_plan` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `plan_name` varchar(100) NOT NULL,
  `plan_type` varchar(50) NOT NULL,
  `category_id` bigint DEFAULT NULL,
  `target_effect` varchar(500) DEFAULT NULL,
  `quadrant_id` bigint DEFAULT 2,
  `priority` int DEFAULT 0,
  `status` tinyint DEFAULT 0,
  `progress` int DEFAULT 0,
  `plan_start_time` bigint DEFAULT NULL,
  `plan_end_time` bigint DEFAULT NULL,
  `actual_start_time` bigint DEFAULT NULL,
  `actual_end_time` bigint DEFAULT NULL,
  `leader_id` bigint DEFAULT NULL,
  `participant_ids` varchar(500) DEFAULT NULL,
  `parent_id` bigint DEFAULT NULL,
  `remark` varchar(500) DEFAULT NULL,
  `create_by` bigint,
  `create_time` bigint,
  `update_by` bigint,
  `update_time` bigint,
  `delete_time` bigint,
  PRIMARY KEY (`id`),
  KEY `idx_parent_id` (`parent_id`),
  KEY `idx_plan_type` (`plan_type`),
  KEY `idx_quadrant_id` (`quadrant_id`)
);

-- 4. 日程表
CREATE TABLE `sys_schedule` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `title` varchar(100) NOT NULL,
  `content` varchar(500) DEFAULT NULL,
  `schedule_type` tinyint DEFAULT 1,
  `all_day` tinyint DEFAULT 0,
  `start_time` bigint NOT NULL,
  `end_time` bigint NOT NULL,
  `location` varchar(200) DEFAULT NULL,
  `quadrant_id` bigint DEFAULT NULL,
  `plan_id` bigint DEFAULT NULL,
  `status` tinyint DEFAULT 0,
  `repeat_type` tinyint DEFAULT 0,
  `remind_minutes` int DEFAULT 15,
  `color` varchar(20) DEFAULT NULL,
  `create_by` bigint,
  `create_time` bigint,
  `update_by` bigint,
  `update_time` bigint,
  `delete_time` bigint,
  PRIMARY KEY (`id`),
  KEY `idx_plan_id` (`plan_id`),
  KEY `idx_start_time` (`start_time`)
);

-- 初始数据
INSERT INTO `sys_quadrant` VALUES
(1, '重要且紧急', 'Q1', '#FF6B6B', '需要立即处理', 1, 1, UNIX_TIMESTAMP()*1000, UNIX_TIMESTAMP()*1000, NULL),
(2, '重要不紧急', 'Q2', '#4ECDC4', '需要规划安排', 2, 1, UNIX_TIMESTAMP()*1000, UNIX_TIMESTAMP()*1000, NULL),
(3, '不重要紧急', 'Q3', '#FFE66D', '可委托他人', 3, 1, UNIX_TIMESTAMP()*1000, UNIX_TIMESTAMP()*1000, NULL),
(4, '不重要不紧急', 'Q4', '#95A5A6', '尽量避免', 4, 1, UNIX_TIMESTAMP()*1000, UNIX_TIMESTAMP()*1000, NULL);

INSERT INTO `sys_plan_type` VALUES
(1, '生活习惯', 'life', '🏃', '#67C23A', 1, 1),
(2, '认知提升', 'cognition', '📚', '#409EFF', 2, 1),
(3, '工作技能', 'skill', '💼', '#E6A23C', 3, 1),
(4, '项目', 'project', '📦', '#909399', 4, 1),
(5, '兴趣爱好', 'hobby', '🎨', '#F56C6C', 5, 1),
(6, '学习', 'study', '🎵', '#8E44AD', 6, 1);
```

### 2.2 后端实体类

#### 任务清单

- [ ] 创建 Quadrant 实体类
- [ ] 创建 PlanType 实体类
- [ ] 创建 Plan 实体类
- [ ] 创建 Schedule 实体类

---

## 第二阶段：核心功能开发（第2-3周）

### 2.1 计划管理功能

#### 任务清单（第2周）

- [ ] Plan Mapper 开发
- [ ] Plan Service 开发
- [ ] Plan Controller 开发（CRUD）
- [ ] Plan Type/Quadrant 查询接口
- [ ] 计划分页查询
- [ ] 计划状态变更接口

#### 接口清单

```
GET  /api/plan/page          分页查询
GET  /api/plan/{id}          详情
POST /api/plan               新增
PUT  /api/plan               修改
DELETE /api/plan/{id}        删除
GET  /api/plan/type/list    计划类型列表
GET  /api/plan/quadrant/list 四象限列表
```

### 2.2 日程管理功能

#### 任务清单（第3周）

- [ ] Schedule Mapper 开发
- [ ] Schedule Service 开发
- [ ] Schedule Controller 开发
- [ ] 日视图接口
- [ ] 周视图接口
- [ ] 月视图接口
- [ ] 日程重复功能

#### 接口清单

```
GET  /api/schedule/page      分页查询
GET  /api/schedule/day       日视图
GET  /api/schedule/week      周视图
GET  /api/schedule/month     月视图
POST /api/schedule           新增
PUT  /api/schedule           修改
DELETE /api/schedule/{id}    删除
```

---

## 第三阶段：前端开发（第4-5周）

### 3.1 计划管理前端

#### 任务清单（第4周）

- [ ] 计划列表页面
- [ ] 计划新增/编辑弹窗
- [ ] 计划筛选功能（分类/象限/状态）
- [ ] 计划详情页
- [ ] 进度条组件

### 3.2 日程管理前端

#### 任务清单（第5周）

- [ ] 日视图页面（小时制）
- [ ] 周视图页面
- [ ] 月视图页面（日历）
- [ ] 日程新增/编辑
- [ ] 日程拖拽功能

### 3.3 四象限视图

#### 任务清单（第5周）

- [ ] 四象限视图页面
- [ ] 象限卡片组件
- [ ] 象限颜色配置

---

## 第四阶段：联动与高级功能（第6周）

### 4.1 计划与日程联动

#### 任务清单

- [ ] 计划转日程接口
- [ ] 批量添加日程接口
- [ ] 前端"添加到日程"按钮
- [ ] 日程完成自动更新计划进度
- [ ] 计划关联日程列表展示

### 4.2 提醒功能

#### 任务清单

- [ ] 日程提醒功能（RabbitMQ/Redis）
- [ ] 计划延期提醒
- [ ] 移动端推送集成

---

## 第五阶段：测试与优化（第7周）

### 5.1 测试阶段

- [ ] 单元测试
- [ ] 集成测试
- [ ] 接口自动化测试
- [ ] 前端功能测试
- [ ] 性能测试

### 5.2 优化

- [ ] 日程查询缓存优化
- [ ] 大数据量分页优化
- [ ] 前端渲染性能优化

### 5.3 上线

- [ ] 部署文档
- [ ] 灰度发布
- [ ] 监控配置

---

## 三、技术依赖

### 3.1 后端依赖

```xml
<dependencies>
    <!-- 已有依赖 -->
    <dependency>
        <groupId>com.know</groupId>
        <artifactId>know-boot-common</artifactId>
        <version>1.0.0</version>
    </dependency>
    
    <!-- 需新增依赖 -->
    <!-- 日程重复计算 -->
    <dependency>
        <groupId>com.google.guava</groupId>
        <artifactId>guava</artifactId>
        <version>31.1-jre</version>
    </dependency>
</dependencies>
```

### 3.2 前端依赖

```json
{
  "dependencies": {
    "@fullcalendar/core": "^6.1.0",
    "@fullcalendar/daygrid": "^6.1.0",
    "@fullcalendar/timegrid": "^6.1.0",
    "@fullcalendar/interaction": "^6.1.0"
  }
}
```

---

## 四、数据库初始化 SQL

```sql
-- 计划管理及日程管理系统数据库初始化脚本
-- 版本: 1.0.0

-- 四象限表
CREATE TABLE `sys_quadrant` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `name` varchar(20) NOT NULL COMMENT '象限名称',
  `code` varchar(20) NOT NULL COMMENT '象限编码',
  `color` varchar(20) NOT NULL COMMENT '显示颜色',
  `description` varchar(200) DEFAULT NULL COMMENT '描述',
  `sort` int DEFAULT 0 COMMENT '排序',
  `status` tinyint DEFAULT 1 COMMENT '状态',
  `create_time` bigint DEFAULT NULL,
  `update_time` bigint DEFAULT NULL,
  `delete_time` bigint DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_code` (`code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='四象限表';

-- 计划类型表
CREATE TABLE `sys_plan_type` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `type_name` varchar(50) NOT NULL COMMENT '类型名称',
  `type_code` varchar(50) NOT NULL COMMENT '类型编码',
  `icon` varchar(100) DEFAULT NULL COMMENT '图标',
  `color` varchar(20) DEFAULT NULL COMMENT '颜色',
  `sort` int DEFAULT 0 COMMENT '排序',
  `status` tinyint DEFAULT 1 COMMENT '状态',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_type_code` (`type_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='计划类型表';

-- 计划表
CREATE TABLE `sys_plan` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `plan_name` varchar(100) NOT NULL COMMENT '计划名称',
  `plan_type` varchar(50) NOT NULL COMMENT '计划分类',
  `category_id` bigint DEFAULT NULL COMMENT '分类ID',
  `target_effect` varchar(500) DEFAULT NULL COMMENT '目标效果',
  `quadrant_id` bigint DEFAULT 2 COMMENT '所属象限',
  `priority` int DEFAULT 0 COMMENT '优先级 0-10',
  `status` tinyint DEFAULT 0 COMMENT '状态: 0-待开始, 1-进行中, 2-已完成, 3-已取消',
  `progress` int DEFAULT 0 COMMENT '进度百分比',
  `plan_start_time` bigint DEFAULT NULL COMMENT '计划开始时间',
  `plan_end_time` bigint DEFAULT NULL COMMENT '计划结束时间',
  `actual_start_time` bigint DEFAULT NULL COMMENT '实际开始时间',
  `actual_end_time` bigint DEFAULT NULL COMMENT '实际结束时间',
  `leader_id` bigint DEFAULT NULL COMMENT '负责人ID',
  `participant_ids` varchar(500) DEFAULT NULL COMMENT '参与人ID列表',
  `parent_id` bigint DEFAULT NULL COMMENT '父计划ID',
  `remark` varchar(500) DEFAULT NULL COMMENT '备注',
  `create_by` bigint DEFAULT NULL,
  `create_time` bigint DEFAULT NULL,
  `update_by` bigint DEFAULT NULL,
  `update_time` bigint DEFAULT NULL,
  `delete_time` bigint DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_parent_id` (`parent_id`),
  KEY `idx_plan_type` (`plan_type`),
  KEY `idx_quadrant_id` (`quadrant_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='计划表';

-- 日程表
CREATE TABLE `sys_schedule` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `title` varchar(100) NOT NULL COMMENT '日程标题',
  `content` varchar(500) DEFAULT NULL COMMENT '日程内容',
  `schedule_type` tinyint DEFAULT 1 COMMENT '日程类型: 1-日程, 2-提醒, 3-节日',
  `all_day` tinyint DEFAULT 0 COMMENT '是否全天: 0-否, 1-是',
  `start_time` bigint NOT NULL COMMENT '开始时间',
  `end_time` bigint NOT NULL COMMENT '结束时间',
  `location` varchar(200) DEFAULT NULL COMMENT '地点',
  `quadrant_id` bigint DEFAULT NULL COMMENT '所属象限',
  `plan_id` bigint DEFAULT NULL COMMENT '关联计划ID',
  `status` tinyint DEFAULT 0 COMMENT '状态: 0-待办, 1-进行中, 2-已完成, 3-已取消',
  `repeat_type` tinyint DEFAULT 0 COMMENT '重复类型: 0-不重复, 1-每日, 2-每周, 3-每月, 4-每年',
  `remind_minutes` int DEFAULT 15 COMMENT '提前提醒分钟数',
  `color` varchar(20) DEFAULT NULL COMMENT '自定义颜色',
  `create_by` bigint DEFAULT NULL,
  `create_time` bigint DEFAULT NULL,
  `update_by` bigint DEFAULT NULL,
  `update_time` bigint DEFAULT NULL,
  `delete_time` bigint DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_plan_id` (`plan_id`),
  KEY `idx_start_time` (`start_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='日程表';

-- 初始数据
INSERT INTO `sys_quadrant` VALUES
(1, '重要且紧急', 'Q1', '#FF6B6B', '需要立即处理', 1, 1, UNIX_TIMESTAMP()*1000, UNIX_TIMESTAMP()*1000, NULL),
(2, '重要不紧急', 'Q2', '#4ECDC4', '需要规划安排', 2, 1, UNIX_TIMESTAMP()*1000, UNIX_TIMESTAMP()*1000, NULL),
(3, '不重要紧急', 'Q3', '#FFE66D', '可委托他人', 3, 1, UNIX_TIMESTAMP()*1000, UNIX_TIMESTAMP()*1000, NULL),
(4, '不重要不紧急', 'Q4', '#95A5A6', '尽量避免', 4, 1, UNIX_TIMESTAMP()*1000, UNIX_TIMESTAMP()*1000, NULL);

INSERT INTO `sys_plan_type` VALUES
(1, '生活习惯', 'life', '🏃', '#67C23A', 1, 1),
(2, '认知提升', 'cognition', '📚', '#409EFF', 2, 1),
(3, '工作技能', 'skill', '💼', '#E6A23C', 3, 1),
(4, '项目', 'project', '📦', '#909399', 4, 1),
(5, '兴趣爱好', 'hobby', '🎨', '#F56C6C', 5, 1),
(6, '学习', 'study', '🎵', '#8E44AD', 6, 1);
```

---

## 五、版本历史

| 版本 | 更新内容 |
|------|----------|
| 1.0.0 | 初始版本，包含完整实施计划 |
